# MVP Required Buildout

## Scope rule

This file now answers two related questions:

1. what still must be built, proven, or cleaned up before the project can honestly claim an MVP
2. what still remains after that for the fuller product described in the original architecture/spec vision

This is a gap map, not a promotion artifact.

Live authority still lives in:

- `audit/remediation/control-plane/CONTROL-PLANE-STATE.yaml`
- `audit/remediation/control-plane/ACTIVE-HANDOFF.md`

If this file conflicts with the control plane, the control plane wins.

## Current audited snapshot

- Last cleared code commit: `93a5ca8406dc0c46c96f0c6285f56ec0ab6601ea`
- Main workspace role: controller/docs only
- Current cleared runtime lane: `Lane H - Retrieval Tool-Surface Authority Expansion`
- Current controller-priority workstream: `Professor-Demo Narrow Lane E Setup`
- Frozen blocked retrieval reference: `f1af7dfafa2e66b831810d70006ab8295411c61b`
- Browser Use classification: `FALLBACK_ONLY`, with a strong secondary `BENCHMARK_OR_CONTROL_ARM_ONLY` role
- Retrospective audit program: complete and cleared, but prerequisite proof only

## What "MVP" means here

For this project, an honest MVP is not "the future fully optimized semantic platform."

It is:

- the core analytical spine working on a real governed path
- governed full-document retrieval over the main research path
- deterministic parse/evidence preparation strong enough to support grounded synthesis
- citations anchored to fetched evidence rather than search snippets
- one honest reviewer/operator workflow
- one truthful canonical runtime surface and run contract

It is not:

- every later optimization from the full research vision
- the whole advanced retrieval stack
- the whole self-improvement/meta-learning stack
- the whole polished consultant platform

## Gap map: current state -> MVP

### 1. Narrow Lane E setup still needs to be packaged, reviewed, and promoted

Status now:

- Lane H is cleared
- article/PDF governed fetch is real on the canonical path
- no new retrieval code lane is currently authorized
- the next live milestone is docs-only setup work, not runtime coding

Why it matters:

- the project now has governed article/PDF fetch, but it still does not have deterministic parse/evidence normalization on the canonical path
- before any more coding happens, the next parse lane needs a narrow setup package so the controller can keep the professor-demo path honest and small

What remains:

- create a docs-only narrow Lane E setup package rooted from `93a5ca8`
- constrain it to article/PDF deterministic parse and evidence normalization only
- adversarially review it
- promote it into the live control plane before any Lane E coding starts

### 2. SEC / EDGAR still needs a separate venue/backend reality decision

Status now:

- explicitly separated from the cleared article/PDF path
- not solved
- not current critical-path work until the article/PDF parse/integration path advances further

Why it matters:

- official filings remain MVP-critical retrieval inputs
- the last live attempt hit real SEC `403` venue/access blocking

What remains:

- classify the SEC issue from the approved post-Lane-H execution venue
- determine whether the blocker is venue-only, backend-path choice, or implementation
- clear official filing acquisition honestly instead of folding it into article/PDF status

### 3. Deterministic parse and evidence normalization still need to be built

Status now:

- designed
- not yet packaged/promoted as the active authorized code lane

Why it matters:

- fetched raw artifacts are not enough by themselves
- MVP requires HTML, filings, PDFs, and papers to become deterministic, source-shaped evidence inputs
- the model should reason over normalized evidence bundles, not raw snippets or raw fetch payloads

What remains:

- parser Lane E
- Lane E setup package
- Lane E promotion decision
- deterministic parse reviews
- evidence normalization reviews
- source-type-specific locators and parse confidence handling

### 4. L1 integration and citation anchoring still need to be built

Status now:

- designed
- not yet authorized as the active code lane

Why it matters:

- even with fetch and parse, the current default L1 path is still too query/snippet shaped
- MVP requires the governed path to become:
  `discovery -> fetch -> parse -> evidence bundle -> synthesis -> anchored citation`

What remains:

- Lane F
- L1 retrieval integration
- citation-anchor integration
- mixed-governance review
- downstream regression review

### 5. Benchmark and acceptance proof still remain

Status now:

- the benchmark/control-arm package is strong on paper
- runtime proof is not complete

Why it matters:

- beating the current shallow path is not enough
- the actual bar is whether the governed retrieval MVP can beat or meaningfully approach manual parallel deep-research control in a truthful evaluation setup

What remains:

- execute the benchmark package after the retrieval core is real
- keep provenance, scoring math, anti-gaming rules, and manual control arm honest
- produce real acceptance evidence rather than docs-only readiness language

### 6. The reviewer/operator story still needs one honest answer

Status now:

- HITL backend/API state machine is real
- premium UI package is strong as a controller handoff
- runtime UI is not implemented

Why it matters:

- future sessions need one honest answer to "what does the reviewer actually use at MVP?"
- the project should not imply a polished consultant UI if the true MVP uses backend/API review plus minimal surfaces

What remains:

- decide whether API-backed reviewer flow is enough for MVP
- if not, build the minimum UI needed for truthful review/runtime visibility
- keep richer UI ambitions clearly later than the MVP bar

### 7. The project still needs one canonical MVP runtime surface

Status now:

- last cleared code truth is `65a612d`
- active forward work is happening through dedicated lane worktrees
- the main workspace is not the canonical runtime

Why it matters:

- a real MVP needs one runtime surface that the docs can point to without caveat

What remains:

- finish the active retrieval buildout
- reconcile accepted work into one canonical runtime line
- give reviewers one front-door run contract and one truthful status story

### 8. Documentation and repo-truth hygiene still matter

Status now:

- much better than before
- still a dirty repo with many worktrees and historical material

Why it matters:

- future sessions can still get confused if front-door docs drift from the control plane
- repo dirt can still make reality look greener or blurrier than it is

What remains:

- keep README/current-state/status docs synchronized with the control plane
- perform cleanup later using the explicit repo-hygiene plan rather than manual deletion

## Where Browser Use belongs

Current recommendation:

- not on the current critical path
- not a replacement for Keystone retrieval
- not the canonical governed fetch path

Best future placement:

- after Keystone's own governed fetch contract is settled
- as a system-owned fallback acquisition backend for hard browser-native cases
- or as a benchmark/control-arm surface for replay/eval/browser-native experiments

What it does not remove:

- deterministic parse
- evidence-bundle contract
- anchored citation contract
- SEC venue classification
- control-plane/tool-surface authority work

So Browser Use is valuable, but it belongs after the core governed retrieval seams are real, not before.

## Fresh-session note

Because long-running chats compact aggressively, the authoritative takeover path for a brand-new controller session is now:

- `audit/remediation/control-plane/CONTROL-PLANE-STATE.yaml`
- `audit/remediation/control-plane/ACTIVE-HANDOFF.md`
- `audit/remediation/control-plane/FRESH-ORCHESTRATOR-BOOTSTRAP.md`
- `audit/remediation/control-plane/FRESH-ORCHESTRATOR-SESSION-PROMPT.md`

Treat this file as a gap map, not as the first authority read.

## Gap map: MVP -> fuller product

These are the bigger product/design goals from the original architecture/spec vision that still remain after an MVP.

Some are researched.
Some are partially scaffolded.
Some are mostly backlog/architecture intent.

### A. Advanced retrieval stack

Still later than MVP:

- embeddings
- `pgvector`
- BM25
- RRF
- rerank
- Docling-style or equivalent richer document processing
- multi-source retrieval federation
- cross-engagement semantic retrieval
- internal document federation

What these add:

- better recall
- better ranking
- better large-corpus retrieval depth
- more reuse of accumulated knowledge

### B. Browser-native fallback acquisition

Still later than MVP-critical governed fetch:

- Browser Use or equivalent browser-native fallback
- JS-heavy and interaction-gated acquisition
- auth/session heavy source access
- richer replay/control-arm browser traces

What this adds:

- resilience on hard pages ordinary HTTP fetch cannot handle cleanly

### C. Observation Library and compounding self-improvement

Still later than MVP:

- Observation Library runtime as a real system, not just plan language
- CBR retrieve/promotion loops
- trajectory storage and trajectory learning
- prompt/skill evolution
- client calibration profiles

What this adds:

- compounding quality gains across engagements
- reusable institutional intelligence

### D. Fuller evaluator stack and calibration

Still later than MVP:

- evaluator calibration against human scoring
- Layers 4-5 evaluator work
- process-trajectory judging
- richer ensemble/judge selection

What this adds:

- stronger trust calibration
- better alignment between automatic and human judgment

### E. Rich output generation

Still later than MVP:

- PowerPoint generation
- Excel/model generation
- PDF export/redraft stack
- richer consultant-ready packaging surfaces

What this adds:

- better deliverable polish and downstream usability

### F. Fuller product UI and collaboration layer

Still later than MVP:

- polished consultant-facing UI
- richer reviewer workflows
- collaborative editing/workspaces
- cross-engagement memory browser
- more mature artifact browsing and run-inspection surfaces

What this adds:

- better usability and team workflow

### G. Production infrastructure and deployment shape

Still later than MVP:

- Temporal migration / hardened orchestration
- stronger auth
- multi-tenancy hardening
- deployment hardening
- operationalized long-running workflows

What this adds:

- robustness at real product scale

### H. Broader analytical product depth from the original design

Still later/backburnered:

- full Observation Library flywheel
- richer causal-inference or advanced analytical agents from the longer-horizon plan
- anything that assumes the retrieval/evidence spine is already stable and trusted

## What has already been solved well enough that it is no longer the main gap

- retrospective trust repair
- control-plane authority discipline
- baseline citation/provenance/evaluator spine through Wave 4B
- honest retrieval/UI planning packets
- one safer workflow-supervisor/control helper

Those were necessary. They are just no longer the main blocker.

## Bottom line

The remaining MVP gap is no longer "invent the system."

It is:

1. finish the governed retrieval buildout honestly
2. prove it against real source acquisition and real review
3. package it into one canonical runtime story

The remaining full-product gap is then everything that compounds, scales, or polishes that core:

- advanced retrieval
- browser-native fallback
- self-improvement/meta-learning
- deeper evaluation
- richer UI/exports
- production infrastructure

## Gap map: MVP -> fuller product

These are the bigger product/design goals from the original architecture/spec vision that still remain after an MVP.

Some are researched.
Some are partially scaffolded.
Some are mostly backlog/architecture intent.

### A. Advanced retrieval stack

Still later than MVP:

- embeddings
- `pgvector`
- BM25
- RRF
- rerank
- Docling-style or equivalent richer document processing
- multi-source retrieval federation
- cross-engagement semantic retrieval
- internal document federation

What these add:

- better recall
- better ranking
- better large-corpus retrieval depth
- more reuse of accumulated knowledge

### B. Browser-native fallback acquisition

Still later than MVP-critical governed fetch:

- Browser Use or equivalent browser-native fallback
- JS-heavy and interaction-gated acquisition
- auth/session heavy source access
- richer replay/control-arm browser traces

What this adds:

- resilience on hard pages ordinary HTTP fetch cannot handle cleanly

### C. Observation Library and compounding self-improvement

Still later than MVP:

- Observation Library runtime as a real system, not just plan language
- CBR retrieve/promotion loops
- trajectory storage and trajectory learning
- prompt/skill evolution
- client calibration profiles

What this adds:

- compounding quality gains across engagements
- reusable institutional intelligence

### D. Fuller evaluator stack and calibration

Still later than MVP:

- evaluator calibration against human scoring
- Layers 4-5 evaluator work
- process-trajectory judging
- richer ensemble/judge selection

What this adds:

- stronger trust calibration
- better alignment between automatic and human judgment

### E. Rich output generation

Still later than MVP:

- PowerPoint generation
- Excel/model generation
- PDF export/redraft stack
- richer consultant-ready packaging surfaces

What this adds:

- better deliverable polish and downstream usability

### F. Fuller product UI and collaboration layer

Still later than MVP:

- polished consultant-facing UI
- richer reviewer workflows
- collaborative editing/workspaces
- cross-engagement memory browser
- more mature artifact browsing and run-inspection surfaces

What this adds:

- better usability and team workflow

### G. Production infrastructure and deployment shape

Still later than MVP:

- Temporal migration / hardened orchestration
- stronger auth
- multi-tenancy hardening
- deployment hardening
- operationalized long-running workflows

What this adds:

- robustness at real product scale

### H. Broader analytical product depth from the original design

Still later/backburnered:

- full Observation Library flywheel
- richer causal-inference or advanced analytical agents from the longer-horizon plan
- anything that assumes the retrieval/evidence spine is already stable and trusted

## What has already been solved well enough that it is no longer the main gap

- retrospective trust repair
- control-plane authority discipline
- baseline citation/provenance/evaluator spine through Wave 4B
- honest retrieval/UI planning packets
- one safer workflow-supervisor/control helper

Those were necessary. They are just no longer the main blocker.

## Bottom line

The remaining MVP gap is no longer "invent the system."

It is:

1. finish the governed retrieval buildout honestly
2. prove it against real source acquisition and real review
3. package it into one canonical runtime story

The remaining full-product gap is then everything that compounds, scales, or polishes that core:

- advanced retrieval
- browser-native fallback
- self-improvement/meta-learning
- deeper evaluation
- richer UI/exports
- production infrastructure
